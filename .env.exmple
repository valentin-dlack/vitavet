# VitaVet API – Example environment variables

# --- Runtime ---
NODE_ENV=development
PORT=3000

# --- CORS ---
# Comma-separated list of allowed origins for the frontend
# e.g. http://localhost:5173,https://staging.vitavet.app
CORS_ORIGINS=http://localhost:5173

# --- Database (PostgreSQL) ---
DB_HOST=127.0.0.1
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=postgres
DB_NAME=vitavet

# --- JWT ---
# Use a strong, secret value in production (32+ random chars)
JWT_SECRET=change-me-in-prod

# --- Notifications / Emails (optional for local dev) ---
# SMTP configuration for sending emails (e.g., reminders)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
# SMTP_USER=your_smtp_username
# SMTP_PASS=your_smtp_password
SMTP_FROM=noreply@vitavet.com

# Frontend base URL (used in notification links)
FRONTEND_URL=http://localhost:5173

# --- Reminders ---
# Disable cron processing in some environments ("1" to disable)
DISABLE_REMINDER_CRON=0

# --- Throttling (optional overrides) ---
# Default values are set in code; uncomment to override
# THROTTLE_TTL_MS=60000
# THROTTLE_LIMIT=100

# Copy this file to .env for local development:
# cp apps/api/env.example apps/api/.env
